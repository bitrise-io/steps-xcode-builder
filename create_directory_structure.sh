if [ ! -d "$BITRISE_LIBRARY_DIR" ]; then mkdir -p "$BITRISE_LIBRARY_DIR"; fi

if [ ! -d "$BITRISE_PROFILE_DIR" ]; then mkdir -p "$BITRISE_PROFILE_DIR"; fi

if [ ! -d "$BITRISE_DEPLOY_DIR" ]; then mkdir -p "$BITRISE_DEPLOY_DIR"; fi