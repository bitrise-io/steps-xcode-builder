if [ ! -d "$CONCRETE_LIBRARY_DIR" ]; then mkdir -p "$CONCRETE_LIBRARY_DIR"; fi

if [ ! -d "$CONCRETE_PROFILE_DIR" ]; then mkdir -p "$CONCRETE_PROFILE_DIR"; fi

if [ ! -d "$CONCRETE_DEPLOY_DIR" ]; then mkdir -p "$CONCRETE_DEPLOY_DIR"; fi